miracle_sort:
  viz_tier: disabled
  reason: "Blocks indefinitely"
bogo_sort:
  viz_tier: disabled
  timeout_ms: 500
  reason: "Non-deterministic, unbounded runtime"
gaslight_sort:
  viz_tier: result_only
trump_sort:
  viz_tier: result_only
thanos_sort:
  viz_tier: full
  reason: "Randomly removes elements each snap"
stalin_sort:
  viz_tier: full
  reason: "Eliminates out-of-order elements"
